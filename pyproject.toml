[project]
name = "tfm"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "canny",
    "warp-lang>=1.15.0",
]

[tool.uv.sources]
canny = { workspace = true }

[tool.uv.workspace]
members = ["src/*"]
